spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    name: postgres
    password: 123456
    url: jdbc:postgresql://localhost:5432/postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
